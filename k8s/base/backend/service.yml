apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: inventory
  labels:
    app: inventory-backend
spec:
  selector:
    app: inventory-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
